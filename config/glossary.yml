# Organization-specific sensitive phrases (English). Longer phrases match first.
terms: []

# Regex patterns (Python regex). Each match is redacted.
patterns:
  - name: INTERNAL_DOC_REF
    pattern: '\\bDOC-\\d{4}-[A-Z]{2}\\b'
  - name: OPERATION_CODE_EXAMPLE
    pattern: '\\bOP-[A-Z]{3}-\\d{3}\\b'
